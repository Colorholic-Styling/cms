{
  "//": "Feature profile for this deployment. One switch per feature: `true` includes its schema fragment (the src/**/schema.sql declaring `-- feature: <id>`) and its code (src/features/<id>/). Run `npm run build` after editing. Turning a feature OFF never drops existing tables \u2014 it only stops creating them on fresh installs. To remove a feature's source too, delete src/features/<id>/ AND its key here; a key listed with no directory and no fragment fails the build. Features do not depend on each other: they cooperate through the extension points in src/core/extensions.ts, so each installs alone and an absent one is simply inert. WARNING: turning off `plugins` removes the /__cms write-back API that installed plugin Workers call, the plugin admin proxy and the manage screen \u2014 any registered plugin stops working, and `plugin-pointer-indexes` must go with it. Turning off `credits` stops every declared cost from being charged; balances, the ledger and the shared pool go with it.",
  "features": {
    "credits": true,
    "i18n": true,
    "jobs": true,
    "media": true,
    "plugin-pointer-indexes": true,
    "plugins": true,
    "runtime-content-types": true,
    "search": true,
    "trash": true,
    "users-roles": true
  }
}
